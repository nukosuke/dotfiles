;;; 006_which-key.el -- キーバインドのガイド表示設定
;;; Commentary:
;;; Code:

(use-package which-key)
(which-key-setup-side-window-right)
(which-key-mode 1)

(provide '006_which-key)
;;; 006_which-key.el ends here
